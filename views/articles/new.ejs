<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
  <div class="container">
    <% if(edit==true){ %>
      <h1>Edit Article</h1>
      <% } else{ %>
        <h1>New Article</h1>
        <% } %>
  </div>
  <div class="container">
    <% if(edit==true){ %>
      <form method="post" action="/articles/updateArticle/<%=article.id%>">
        <% } else{ %>
          <form method="post" action="/articles/sendArticle">
            <% } %>
              <div class="form-label">
                <label for="title" class="form-label">Title</label>
                <input type="text" name="title" id="title" value="<%= article.title %>" class="form-control" required>
              </div>
              <div class="mb-3">
                <label for="description" class="form-label">Description</label>
                <textarea class="form-control" name="description" id="description" required
                  rows="2"><%= article.description %></textarea>
              </div>
              <div class="mb-3">
                <label for="markdown" class="form-label">Markdown</label>
                <textarea class="form-control" name="markdown" id="markdown" required
                  rows="3"><%= article.markdown %></textarea>
              </div>
              <div>
                <a class="btn btn-secondary" href="/articles/index">cancel</a>
                <button type="submit" class="btn btn-success">Save</button>
              </div>
          </form>

  </div>
</body>

</html>